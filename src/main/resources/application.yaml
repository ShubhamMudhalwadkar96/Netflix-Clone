spring:
  application:
    name: com.netflix.clone
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pulsescreen_video
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 2GB
      max-request-size: 2GB

server:
  tomcat:
    max-swallow-size: -1
    max-http-form-post-size: -1

file:
  upload:
    video-dir: uploads/videos
    image-dir: uploads/images

  mail:
    host: smtp.gmail.com
    port: 587
    username: shubham.mudhalwadkar96@gmail.com
    password: Unknown007@
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  frontend:
    url: http://localhost:4200

#jwt:
  #secret: testjwt

